#!/usr/local/bin/php
<?php

declare(strict_types=1);

use App\FinYahoo;

require dirname(__DIR__) . '/vendor/autoload.php';

$date = (new DateTimeImmutable())->format(DATE_ATOM);
$sum = FinYahoo::sum(1, 2, 3);

echo "The date: {$date}" . PHP_EOL;
echo "The sum: {$sum}" . PHP_EOL;
